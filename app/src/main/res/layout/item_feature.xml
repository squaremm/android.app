<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp" >

    <ImageView
        android:id="@+id/itemFeatureCheckmark"
        android:layout_width="@dimen/featureItemImageSize"
        android:layout_height="@dimen/featureItemImageSize"
        android:src="@drawable/checkmark"
        android:tint="@color/nice_pink" />

    <TextView
        android:id="@+id/itemFeatureText"
        android:textStyle="bold"
        android:textColor="@android:color/black"
        android:textSize="@dimen/featureItemTextSize"
        android:layout_marginStart="5dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:includeFontPadding="false"
        tools:text="15 available bookings" />

    <TextView
        android:id="@+id/itemFeatureTextSecondary"
        style="@style/Models14Text"
        android:textColor="@color/secondary_text"
        android:textSize="@dimen/featureItemSecondaryTextSize"
        android:layout_marginStart="3dp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:includeFontPadding="false"
        tools:text="(gym excluded)" />

</LinearLayout>